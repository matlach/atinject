package org.atinject.api.role.enumeration;

public enum DefaultRoles implements Roles {

    // weblogic : admin, operator, deployer, monitor, app tester,anonymous, others
    
    GUEST,
    REGISTERED,
    ADMIN,
    SUPER_ADMIN;

}
